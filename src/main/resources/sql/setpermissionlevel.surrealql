let $x = select value user from only type::record("mindustry_profile", <uuid> $id);
update $x set mindustry_permission_level = $permissionLevel;
return fn::mindustry_update_profile(type::record("mindustry_profile", <uuid> $id));